# === RotaSpeed Prime — frontend ===
# Em produção (app nas lojas) use a URL do backend na NUVEM, nunca localhost.
VITE_API_BASE_URL=https://rotaspeed-api.onrender.com
VITE_API_KEY=

# Supabase (auth/banco/storage) — use a URL e a anon key públicas do seu projeto.
VITE_SUPABASE_URL=https://SEU_PROJETO.supabase.co
VITE_SUPABASE_ANON_KEY=coloque_a_anon_key_aqui
